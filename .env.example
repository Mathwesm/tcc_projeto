# Copy to .env and fill in. The .env file is never committed.
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_SERIALIZE=false

# Failure alerts. Create the bot with @BotFather, then message it once and read
# your chat id from https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
